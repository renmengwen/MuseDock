# 2026-06-19 工作日志

## Review：html-video Frame Project Contract 设计文档

对 `docs/superpowers/plans/2026-06-19-html-video-frame-project-contract.md` 进行了系统性代码 + 文档 review。

**主要发现：**
- B1（阻塞）：`normalizeFrame` 新逻辑会与现有 schema 测试（`graph_node_id === null` 断言）冲突，需先改测试。
- B2（阻塞）：`captionLayer.normalizeCaptionsForFrame` 与 `rawHtmlFrameBuilder.normalizeCaptions` 重复，需明确去留（建议改为 re-export）。
- B3（阻塞）：Task 4 导出前强制 materialize 时 templateRegistry 传递路径未说明，raw_html only 工程会 throw。
- I1（重要）：`applyFramePatch` 的 duration early return 会导致同时传入的 captions 丢失，需调整处理顺序。
- I5（重要）：`CaptionsPanel` 保存字幕时缺少 `selectedFrameId`，需从 `HtmlVideoProjectEditor` 传入。
- 现有全部相关测试（schema/materializer/edit-patch/raw-html-frame-builder）均绿色通过。
- 设计方向与参考工程 html-video core 模式对齐，整体可执行。
