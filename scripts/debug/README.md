# Debug Scripts

This directory contains manual debugging scripts. They may open Chrome, require a running local server, depend on real login state, or access external sites.

These scripts are intentionally not included in `npm test`.
