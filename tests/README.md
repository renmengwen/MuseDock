# Tests

This directory contains automated Node test scripts that are safe to run from `npm test`.

Run the full suite:

```powershell
npm test
```

Run a focused test:

```powershell
node tests/test-ai-text-model.js
```
